{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "node src/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "//": "Node version is pinned in backend/.node-version, which both Railpack and Nixpacks honour. No builder is declared here: Railway builds this service with Railpack, and asserting NIXPACKS only made the config lie about what actually runs."
}
